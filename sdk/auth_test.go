package sdk_test

import (
	"testing"
)

func Test_Logout(t *testing.T) {
	t.Skip() // TODO: rewrite this test to perform its own independent logon
	// getAuth()

	// res, err := pcc.Logout(context.Background())
	// require.NoError(t, err)
	// assert.Equal(t, 0, res.Result)
}
